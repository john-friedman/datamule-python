from datamule.rewrite.submissions import Submission

submission = Submission('f/')

for doc in submission.document_type('8-K'):
    print(doc)
	# do something to document
	
submission.keep('8-K','EX-22.1')
submission.drop('GRAPHIC')